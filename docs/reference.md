# Reference

## clean:

$clean ( ) \rightarrow undefined$

The `clean` command is a Genie task that securely deletes the `build` directory using the Masonry asset pipeline. 

### Arguments

This command accepts no arguments.

### Returns

- `undefined` (Outputs progress or errors to the terminal)

### Example

```bash
npx genie clean
```
