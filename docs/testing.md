# Testing

Testing for Genie Clean uses the `@dashkite/amen` testing framework to verify that the task runner integration works correctly. 

To execute the test suite, use the Genie task runner:

```bash
npx genie test
```
